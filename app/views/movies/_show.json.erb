  { "id" : <%= raw(movie.id.to_json) %>,
    "name" : <%= raw(movie.name.to_json) %>,
	"director" : <%= raw(movie.director.to_json) %>,
	"writer" : <%= raw(movie.writer.to_json) %>,
	"release_date" : <%= raw(movie.release_date.to_json)%>,
	"storyline" : <%= raw(movie.storyline.to_json), %>
  }